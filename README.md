# baseline-browserslist

Just having a play with LightningCSS + Browserslist to test out using the Baseline configurations ala https://web.dev/articles/use-baseline-with-browserslist

Within `vite-project` you can `npm i` and `npm run dev` to see it in action.

Then the `style.css`
